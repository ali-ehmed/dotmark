<h2><small>Account Settings</small></h2>
<%= simple_form_for(@resource, url: update_account_path(params[:username]), html: { method: :put }) do |f| %>
  <%= f.error_notification if @profile_error %>

  <div class="form-inputs">
    <%# if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <!-- <p>Currently waiting confirmation for: <%#= resource.unconfirmed_email %></p> -->
    <%# end %>
    <%= f.input :current_password, label: "Old password", hint: "we need your old password to confirm your changes", required: true %>
    <%= f.input :password, autocomplete: "off", hint: "leave it blank if you don't want to change it", required: false %>
    <%= f.input :password_confirmation, required: false %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, "Save" %>
  </div>
<% end %>

<!-- <h3>Cancel my account</h3> -->

<!-- <p>Unhappy? <%#= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p> -->

<!-- <%= link_to "Back", :back %> -->